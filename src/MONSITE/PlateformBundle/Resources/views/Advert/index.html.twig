
{% extends "MONSITEPlateformBundle::layout.html.twig" %}

{% block title %} Accueil - {{parent()}}{% endblock %}


{% block MONSITEPLATEFORM_body %}
    
    <h2>Liste des annonces</h2>
    
    <ul>
         {% for message in app.session.flashbag.get('info') %}
             <p> Message flash: {{ message }} </p>
         {% endfor %}
         
        {% for advert in adverts %}
            <li>
                
                <a href=" {{ path('MONSITE_platform_view', {'id': advert.id })  }} "> {{ advert.title }}</a>
                par {{advert.author}}
                le {{advert.date|date('d/m/Y')}}
                
            </li>
        {% else %}
            <li>Pas d'annonces</li>
        {% endfor %}
    </ul>
    
{% endblock %}
        

















{#
        <h1>Hello World</h1>
        
        <p>
            Hello world, le premier programme à écrire quand on
            débute en prog
            
            Nom passé en paramètre: {{ nom }}
            
        </p>
        
        <p>Lien venant du template: <a href=" {{path('MONSITE_platform_view_slug', {'year' : 2015, 'slug' : nom, 'format': "xml"} ) }} ">Lien vers la page Slug</a>  </p>
        <p>Lien venant du controller: <a href="{{ urlSlug }}" > Lien vers Slug </a></p>
        <p>Lien pour accéder à la partie add: <a href=" {{path('MONSITE_platform_add')}} " >Zone add</a> </p>

#}