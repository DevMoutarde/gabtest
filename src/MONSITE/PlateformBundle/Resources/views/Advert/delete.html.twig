{# empty Twig template #}


{% extends "MONSITEPlateformBundle::layout.html.twig" %}

{% block title %}
    Suppression d'articles - {{ parent() }}
    
{% endblock %}

{% block MONSITEPLATEFORM_body %}
    <h2>Suppression de l'article</h2>
    
    <p>
        Voulez vous supprimer cet article "{{ advert.title }}"?
    </p>
    
    <form action=" {{ path('MONSITE_platform_delete', { 'id': advert.id}) }} " method="POST">
        
        <a href=" {{ path('MONSITE_platform_view', {'id': advert.id}) }} " class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i>
            Retour à l'annonce
        </a>
            
            <input type="submit" value="supprimer" class="btn btn-danger"/>
         {# generation du champ CSRF #}
        {{ form_rest(form) }}
    
    </form>
    
{% endblock %}
